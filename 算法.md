# 找歌算法说明

## 1. 输入与目标

输入：

- 珠子库存：26 个字母，每个字母要么不限量，要么有一个数量上限。
- 歌曲池：每首歌是一个 A-Z 字母多重集（已忽略空格、标点，含数字或无法拼写的条目已被排除）。
- 用户偏好：主要消耗珠子、不想消耗珠子。

两个核心问题：

1. **拼最多歌曲**：在库存内选一个歌曲子集，使歌曲数量最大。
2. **消耗珠子**：优先消耗"主要消耗"珠子、避免"不想消耗"珠子。

## 2. "拼最多歌曲"候选算法

### 2.1 暴力枚举

- 做法：枚举所有歌曲子集，检查库存是否够用。
- 复杂度：O(2^N)，N 为歌曲数（约 300 条），指数级，不可行。
- 优点：能保证全局最优。
- 缺点：只适合教学，不适合实际网站。

### 2.2 动态规划（精确）

- 做法：把库存看作一个 26 维背包，状态是"每个字母剩多少"，用 DP 记录最大歌曲数。
- 复杂度：状态数 = 各字母库存乘积。库存大时状态爆炸；不限量时无法枚举。
- 优点：理论最优。
- 缺点：26 维背包在浏览器里基本跑不动，且"不限量"场景无法建模。
- 结论：不采用，仅作为理论参照。

### 2.3 贪心：先拼短的

- 做法：每轮挑"所需珠子总数最少"且当前库存能拼的歌，拼完扣掉珠子，继续下一轮。
- 复杂度：O(N × 字母数)，毫秒级。
- 优点：极快；短歌通常能用更少珠子，容易得到较高的歌曲数量。
- 缺点：局部最优不等于全局最优。例如先拼两条短的可能会挡住"一条较长但能把库存耗得更干净"的组合。

### 2.4 贪心 + 局部搜索（采用的方案）

做法分三步：

1. **贪心起步**：按"珠子总数少优先、字母种类少优先"的顺序拼出一组候选解。
2. **局部搜索**：在候选解上做交换和替换。尝试把某首已在方案里的歌换成不在方案里的歌，如果总数不变但剩余珠子更多，就接受；再尝试加入新歌，直到无法再加入。
3. **随机重试**：用不同排序和随机扰动重复多次，取歌曲数量最大的一组。

- 复杂度：数百毫秒内完成。
- 优点：不依赖 26 维状态；不限量库存也能处理；结果接近最优；能同时生成多个候选方案。
- 缺点：不保证数学意义上的全局最优。

## 3. 组合（Combo）机制

界面上的"组合 1 / 组合 2 / 组合 3…"按以下规则生成：

1. 组合 1 = 当前库存下，算法找到的一组"数量最大"歌曲集合。
2. 用户从组合 1 选 1 首后，扣掉这首歌消耗的珠子，**用剩余珠子重新计算组合 2**。
3. 组合 3 同理，在组合 1、2 选定后继续递推。

因此：

- 同一首歌可以出现在不同组合里，因为它在不同剩余库存下都可能可选。
- 组合内歌曲互斥（同一组只能选 1 首）；组合之间串联成一套手链方案。
- "数量最大"是每次在**当前剩余库存**下的最大，而不是一开始就锁死一个总方案，这样可以边选边让你挑喜欢的歌。
- 如果用户在某组合里不选最优的那首，后续组合会按实际剩余珠子重新计算，不保证之前的全局最大数量，但每个组合都保证"在当前选择下，尽量多拼"。

## 4. "消耗珠子"模式

### 4.1 单首歌曲评分

每首歌按所需珠子打分：

```
score = 2 × 主要消耗珠子数 - 2 × 不想消耗珠子数 + 0.01 × 总珠子数
```

- 权重和公式可以后续调，让"想消耗"和"不想消耗"的优先级可配置。
- 最后一项只是平手时让更长的歌靠前。

### 4.2 贪心搭配

单首评分之外，额外提供"连续搭配"：

1. 每轮挑当前库存下分数最高的歌，拼完扣珠子。
2. 重复直到没有能拼的歌或用户停止。
3. 支持撤销最后一首，方便人工调整。

这是对"在消耗偏好下选一套手链"的贪心近似；它不是背包最优，但速度快、结果直观，适合让用户边看边改。

## 5. 边界与细节

- 含数字的条目（如 `1989`、`22`、`the 1`）直接从可拼池排除，不参与任何模式。
- 后缀相同的歌合并：`All Too Well (Taylor's Version)` 与 `All Too Well` 视为同一首。
- 库存不限量时，"拼最多歌曲"没有约束上限，组合 1 会列出所有可拼条目；用户选完后，剩余珠子变为有限库存，再算组合 2。
- 缩写条目（如 `RWYLM`）与原名（`right where you left me`）是两条独立可拼条目，不互相换算。
