# 友谊手链网站规划

> 状态：待你审阅。确认后再进入正式开发。
> 仓库：`iqila0629-gif/friendship`（尚未创建远程仓库，创建时需要 GitHub 授权）

## 1. 一句话目标

把"手上有哪些字母珠子"变成输入，自动算出**现在能拼出哪些 Taylor Swift 相关条目**，并按"最多能拼几首""主要消耗哪些珠子"等方式给出答案。

## 2. 可拼内容池

| 来源 | 内容 | 示例 |
| --- | --- | --- |
| 1. 歌曲/专辑名 | 歌曲 + 对应专辑名 | `cardigan`、`folklore` |
| 2. 缩写/变种 | 4 个单词及以上的歌名自动取首字母；You 单独成词时缩成 U；特例 ATW / rep / folkmore / mid | `right where you left me → RWYLM`、`You Belong With Me → UBWM` |
| 3. 关键歌词 | 仅使用你提供的 4 条 | `salt air`、`rust on your door`、`burning red`、`fuck the patriarchy / fxxk the patriarchy` |
| 4. 相关单词 | 仅使用你提供的 4 条 | `Taylor Swift`、`TS`、`eras tour`、`eras` |

数据基座来自 [shaynak/taylor-swift-lyrics](https://github.com/shaynak/taylor-swift-lyrics) 的 `songs.csv`（歌名、专辑、歌词），已确认共 269 首、25 个专辑来源，你列的 12 张专辑全部覆盖。

### 拼写规则（会写进产品说明）

- 不分大小写，忽略空格和标点。
- 珠子只有 A-Z 26 种；撇号、数字、`!`、`?`、`...` 等不需要珠子。
- 如 `Slut!` 只需要 `SLUT`；`...Ready for It?` 只需要 `READYFORIT`。
- `fxxk the patriarchy` 按字面拼 `FXXKTHEPATRIARCHY`。
- **含数字的一律不考虑**：`1989`、`22`、`the 1` 等不会出现在可拼列表里。
- **后缀不算新歌**：`(Taylor's Version)`、`(10 Minute Version)`、`[From The Vault]` 等只取主标题，有无后缀算同一首，如 `All Too Well` 两种版本合并为一条。

## 3. 核心功能

### 3.1 选珠子（输入）

- **正选**：点亮的字母 = 手上有。
- **反选**：选的是手上**没有**哪些字母；同样用点亮表示（界面会区分两种状态的视觉）。
- 每个字母可选填数量，不填 = 该字母不限量；填了 = 精确库存。
- 可标记"主要消耗"和"不想消耗"两类珠子（影响消耗模式排序，不影响"能不能拼"）。
- 提供一键全选 / 清空 / 恢复默认。

### 3.2 找歌（筛选）

- 内容类型可勾选：歌曲/专辑名、缩写变种、关键歌词、相关单词。
- 专辑筛选：12 张专辑 + "其他"；`其他` 收纳 Beautiful Eyes、单曲、影视原声、合作曲等未入正专的条目。
- 普通模式：只判断"能不能拼出来"，展示所有可拼条目，按字母数/专辑排序。

### 3.3 拼最多歌曲模式

- 用当前库存求"最多能拼几首"，展示组合 1、组合 2、组合 3…；同一首歌可以出现在不同组合里，目的是**保证数量最大的前提下，让你能挑自己喜欢的歌**。
- 每个组合内歌曲互斥（同一组里只能选 1 首）；选完组合 1 后，组合 2 按剩余珠子重新计算，不够的歌曲变暗，亮着的都可以接着选。
- 该模式的歌曲栏很紧凑：每个候选是一枚圆形小珠子，只写歌曲名，不展示所需字母明细。

### 3.4 消耗珠子模式

- 优先选"主要消耗"珠子多、且少碰"不想消耗"珠子的条目。
- 展示每条消耗明细：消耗了哪些珠子、主要/不想占比。
- 额外提供"贪心搭配"：不断挑当前分数最高的条目，拼完一组后看剩余珠子，可一键撤销最后一首。
- 该模式的歌曲栏同样使用圆形小珠子，只写歌曲名。

## 4. 结果样式

- 每条结果是一个"珠子卡片"：框内是条目文字，**填充色 = 专辑代表色**；普通模式显示所需珠子计数，拼最多/消耗模式只显示歌曲名。
- 被选中的条目变成"已串上"状态，剩余珠子数实时更新。
- 缩写条目单独显示原词（如 `RWYLM (right where you left me)`）。
- 普通模式在卡片上展示字母计数，如 `2A 1B`；拼最多/消耗模式不展示字母明细。
- 正式网站不单独展示"专辑代表色"图例；颜色只作为歌曲卡片背景存在。

### 专辑代表色

| 专辑 | 代表色 |
| --- | --- |
| Taylor Swift | 浅绿 |
| Fearless | 黄 |
| Speak Now | 浅紫 |
| Red | 红 |
| 1989 | 天蓝 |
| reputation | 黑 |
| Lover | 粉 |
| folklore | 深灰 |
| evermore | 褐 |
| Midnights | 深蓝 |
| TTPD | 白（深色文字） |
| The Life of a Showgirl | 橙 |
| 其他 / 未收录 | 浅灰 |

> 注：代表色只用于歌曲卡片背景，正式网站不展示色块图例；上表是开发参照。

## 5. 页面结构（单页应用）

```
┌────────────────────────────────────────────┐
│ 顶栏：网站名 / 库存摘要 / 重置              │
├───────────────┬────────────────────────────┤
│ 选珠子面板     │ 结果区                     │
│ 26 个字母珠子  │ 模式切换                    │
│ 正选/反选      │ 内容类型筛选 / 专辑筛选      │
│ 数量输入       │ 歌曲卡片流 / 组合卡片        │
│ 主要/不想消耗  │ 珠子消耗明细                │
├───────────────┴────────────────────────────┤
│ 底部：数据来源与拼写规则说明                 │
└────────────────────────────────────────────┘
```

移动端改为上下两段：先选珠子，再往下看结果。

## 6. 技术方案

- **纯前端静态站**：HTML + CSS + JavaScript，无后端，可直接部署 GitHub Pages。
- 数据用脚本从 `songs.csv` 清洗成一份 `songs.json` 提交进仓库；网站运行时只读这份 JSON。
- 数据预处理脚本保留在仓库里，方便日后更新歌曲库后重新生成。
- 核心算法（库存判断、多首组合、贪心搭配）为纯函数，便于测试。

## 7. 开发阶段

1. **数据层**：导入歌曲库、映射专辑、生成缩写、加入关键歌词与相关词，输出 JSON。
2. **选珠子交互**：字母网格、正反选、数量、主要/不想消耗。
3. **普通模式**：库存判断 + 筛选 + 专辑色卡片。
4. **拼最多模式**：组合生成 + 联动变暗。
5. **消耗模式**：打分排序 + 贪心搭配 + 撤销。
6. **视觉打磨与响应式**：移动端、空状态、无结果提示。
7. **部署**：初始化 git、创建远程仓库、推送、开启 GitHub Pages。

## 8. 已确认的决策

1. 缩写只取主标题，后缀不算；有无后缀算同一首。
2. "拼最多歌曲"按近似最优实现，算法方案和优缺点单独写在 `算法.md`。
3. 远程仓库已创建并推送。
